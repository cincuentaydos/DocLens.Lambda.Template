environment = "prod"
aws_region  = "eu-west-1"

# Fill in once you've bought the domain (see ADR-009 / infra/README.md).
# prod is served at the bare domain_name.
domain_name        = "REPLACE_ME.example"
create_hosted_zone = true
