meta {
  name: Swagger Health
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/swagger/v1/swagger.json
  body: none
  auth: none
}

tests {
  test("API is reachable", function () {
    expect(res.getStatus()).to.equal(200);
    expect(res.body.info.title).to.equal("DocLens.Lambda");
  });
}
